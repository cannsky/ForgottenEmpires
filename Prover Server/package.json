{
  "name": "prover-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --outfile=build/index.js --bundle --format=esm --packages=external",
    "start": "npm run build && node build/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "contracts": "file:../contracts",
    "esbuild": "^0.19.5"
  },
  "devDependencies": {
    "@types/node": "^20.9.0"
  }
}