{
    "name": "client",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "build": "esbuild src/index.ts --outfile=build/lib.js --bundle --minify --format=esm",
        "build-worker": "esbuild src/worker.ts --outfile=build/worker.js --bundle --minify --format=esm",
        "start": "npm run build && node build/index.js"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "contracts": "file:../contracts",
        "esbuild": "^0.19.5",
        "express": "^4.18.2"
    },
    "devDependencies": {
        "@types/node": "^20.9.0",
        "@types/express": "^4.17.21"
    }
}